server:
  port: 8084

spring:
  jpa:
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

staffjoy:
  account-service-endpoint: http://localhost:8090
  company-service-endpoint: http://localhost:8082
